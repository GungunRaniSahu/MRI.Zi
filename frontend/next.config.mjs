/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Backend base URL is read client-side from NEXT_PUBLIC_API_URL (defaults to localhost:8000).
};

export default nextConfig;
